from django.apps import AppConfig
"""
corresponding to seeting.py in project level(global level), this is the configuration file in app level
"""

class InstaappConfig(AppConfig):
    name = 'InstaApp'
